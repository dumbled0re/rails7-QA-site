<%= form_with model: @question do |form| %>
<%
=begin%>
@question.errors.any?でエラーがあるかどうかを判定しています。(422エラーがあるかどうかを判定しています。)
full_messagesにエラー内容が配列で入っているので、eachで回して表示しています。
<%
=end%>
  <% if @question.errors.any? %>
    <div>
      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :content %><br>
    <%= form.text_area :content %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>